# Azahar config backup

This is a backup of my azahar config and saves/data folder. 
to use ln or copy the contents of config to

        $HOME/.config/azahar-emu

and the contents of data to 

        $HOME/.local/share/azahar-emu
